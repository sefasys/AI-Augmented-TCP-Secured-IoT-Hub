It's a chatroom for company employees to check status of the IoT devices in the work area with fine tuned AI model that we trained with a dataset,
It supports multiple users, so that also employees can communicate with each other in a TCP secured way. 
